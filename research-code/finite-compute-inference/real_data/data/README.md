This directory holds raw and prepared inputs expected by the analysis scripts.
Nothing here is tracked. Check each file against `../data_checksums.json`.

The public snapshot does not yet include every raw-to-prepared transformation;
see the real-data status section in the package README before attempting a rerun.
