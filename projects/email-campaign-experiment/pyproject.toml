[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "hillstrom-email-experiment"
version = "1.0.0"
description = "A reproducible decision analysis of the public Hillstrom email experiment"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["src"]

